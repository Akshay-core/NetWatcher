# NetWatcher — Core dependencies
# Install: pip install -r requirements.txt

# Scanning
python-nmap>=0.7.1      # Nmap Python wrapper (requires nmap binary)

# CLI
typer>=0.9.0            # Typed CLI framework
rich>=13.0.0            # Terminal styling

# Reports
jinja2>=3.1.0           # HTML templating

# AI (Ollama runs separately — no Python package needed)
# Install Ollama: https://ollama.ai
# Then: ollama pull mistral:7b-instruct-q4_K_M
#
# Nmap binary:
#   Linux/Mac : sudo apt install nmap  OR  brew install nmap
#   Windows   : https://nmap.org/download.html
